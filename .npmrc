# Hoisted layout + short absolute virtual store so CMake/Ninja paths stay under Windows limits.
# If pnpm install errors on permissions, create C:\wpstore as your user (or pick another short path).
node-linker=hoisted
virtual-store-dir=C:/wpstore
