# Hoisted layout so React Native / Expo autolinking resolves native modules on EAS (Linux).
# Do not set virtual-store-dir to a Windows path (e.g. C:/wpstore) — that breaks cloud Android builds.
node-linker=hoisted
